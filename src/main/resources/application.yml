spring:
  datasource:
    url: jdbc:mariadb://localhost:3307/task5
    username: root
    password: 1234
    initialization-mode: always  # schema.sql ?? data.sql ?? ??

  jpa:
    hibernate:
      ddl-auto: update  # ???: create, create-drop, update ??
    show-sql: true
    properties:
      hibernate:
        format_sql: true


  # Thymeleaf ??
  thymeleaf:
    cache: false  # ??? ??? ?? ???? (Hot Reload)


  messages:
    basename: messages/ValidationMessages,messages/i18n/messages
    encoding: UTF-8

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true



